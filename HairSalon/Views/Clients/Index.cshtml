@{
  Layout = "_Layout";
}
@using System.Collections.Generic
@using HairSalon.Models
@model List<HairSalon.Models.Client>;

<h2>View Clients</h2>
<hr />
@if (@Model.Count == 0)
{
  <h3>You haven't added any clients yet!</h3>
  <p>Click below to add a new client.</p>
}
  @foreach (Client client in Model)
  {

      <p>@Html.ActionLink($"{client.Name}", "Details", new { id = client.ClientId})</p>
  }
</div>
<hr /> 
<a href='@Url.Action("Create")'><button>Add Client</button></a>
<a href='@Url.Action("Index", "Home")'><button>Home</button></a>